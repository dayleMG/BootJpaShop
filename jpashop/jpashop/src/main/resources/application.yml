spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopinfo_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: mg1004
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
